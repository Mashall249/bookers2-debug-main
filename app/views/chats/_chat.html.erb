<div class ="right-container d-flex justify-content-end">
  <p id ="chat_<%= chat.id %>" style="background-color: rgba(0, 185, 0,0.6); paddong: 5px; border-radius: 10px;"><%= chat.message %></p>
  <%= button_to "Delete", chat_path(chat), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger btn-sm ml-2", remote: true, id: "delete_chat_#{chat.id}" %>
</div>