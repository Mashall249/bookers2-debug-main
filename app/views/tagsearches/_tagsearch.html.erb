<div class="search_form">
  <%= form_with url: tagsearches_search_path,method: :get, local: true do |f| %>
    <td><%= f.text_field :content %></td>
    <td><%= f.submit'Search tag' %></td>
  <% end %>
</div>